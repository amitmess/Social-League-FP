<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/Theme.SocialLeagueFP">

        <!-- מסך פרטים -->
        <activity
                android:name=".ui.details.MatchDetailsActivity"
                android:exported="false" />

        <!-- מסך התחברות (מסך פתיחה) -->
        <activity
                android:name=".ui.auth.LoginActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- מסך רשימה -->
        <activity
                android:name=".ui.matches.MatchesListActivity"
                android:exported="false" />

        <!-- מסך פרופיל משתמש -->
        <activity
                android:name=".ui.profile.UserProfileActivity"
                android:exported="false" />

        <!-- מסך מיקום אצטדיון -->
        <activity
                android:name=".ui.location.StadiumLocationActivity"
                android:exported="false" />
    </application>

</manifest>
